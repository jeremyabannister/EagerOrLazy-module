import XCTest
@testable import EagerOrLazy_module

final class EagerOrLazy_moduleTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(EagerOrLazy_module().text, "Hello, World!")
    }
}
